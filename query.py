import sys

def single_url_recommendation(path):
    pass

def build_model(path):
    pass

def is_url(path):
    pass

path = sys.argv[1]
if is_url(path):
    single_url_recommendation(path)
else:
    build_model(path)
